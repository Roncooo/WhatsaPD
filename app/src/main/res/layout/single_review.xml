<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="20dp"
    app:cardElevation="5dp"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="yes"
        android:orientation="vertical"
        android:padding="15dp">

        <TextView
            android:id="@+id/review_username"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:accessibilityTraversalBefore="@id/review_rating_bar"
            android:padding="10dp"
            android:text="@tools:sample/full_names"
            android:textColor="@color/review"
            android:textSize="@dimen/review_username" />

        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">

            <RatingBar
                android:id="@+id/review_rating_bar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:importantForAccessibility="no"
                android:isIndicator="true"
                android:numStars="5"
                android:progressTint="@color/ratingbar_stars"
                android:secondaryProgressTint="@color/backgrund_stars"
                android:stepSize="1"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Used to let the screen readers correctly describe RatingBar.
             If set to visibility="invisible" (instead of color transparent) it
             would be ignored by the screen reader -->
            <TextView
                android:id="@+id/review_rating_bar_text_description"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:layout_alignLeft="@id/review_rating_bar"
                android:layout_alignTop="@id/review_rating_bar"
                android:layout_alignRight="@id/review_rating_bar"
                android:layout_alignBottom="@id/review_rating_bar"
                android:textColor="@android:color/transparent"
                tools:ignore="RtlHardcoded" />

        </RelativeLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/review_date_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/review_date_text"
                android:textColor="@color/review_date" />

            <TextView
                android:id="@+id/review_date"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@tools:sample/date/ddmmyy"
                android:textColor="@color/review_date" />

        </LinearLayout>

        <TextView
            android:id="@+id/review_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:scrollHorizontally="false"
            android:text="@tools:sample/lorem[30]"
            android:textColor="@color/review"
            android:textSize="@dimen/review_text" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
